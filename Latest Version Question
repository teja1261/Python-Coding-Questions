Write a program to find which is the Latest version between V and S.

Note: Usually Version is number.number

Ex : 2.1 , 3.2.10, 2.4.7.11

Input:
2.2.4
2.2.1

Output:
2.2.4

code:

V = tuple(map(int,input().split('.')))
S = tuple(map(int,input().split('.')))

if V > S:
    number = [str(i) for i in V]
else:
    number = [str(i) for i in S]

print(".".join(number))
